name: http request dummy json
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow-dispatch:

jobs:
  request:
    runs-on: ubuntu-latest
    steps:
      run: curl https://dummyjson.com/products/1
